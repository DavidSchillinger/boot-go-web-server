Boot.dev Go HTTP Server course code

Generate SQL-Go queries: `sqlc generate`

Run SQL migrations (up): `goose postgres <DATABASE_URL> up`

Run SQL migrations (down): `goose postgres <DATABASE_URL> down`
